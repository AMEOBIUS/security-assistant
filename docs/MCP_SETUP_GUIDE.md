# MCP Configuration Guide (Updated)

## 📋 Обновленная конфигурация

### Изменения:
1. ✅ Добавлен **Tavily MCP** (нативный SSE сервер)
2. ✅ Сохранен **unified-search** (DuckDuckGo fallback)
3. ❌ Удален **Perplexity** (не работает)

---

## 🚀 Быстрая установка

### Шаг 1: Скопируй конфиг
```cmd
copy mcp.json.template "C:\Users\admin\AppData\Roaming\GitLab\duo\mcp.json"
```

### Шаг 2: Заполни API ключи

Открой `C:\Users\admin\AppData\Roaming\GitLab\duo\mcp.json`:

#### 🔍 Tavily (нативный MCP - рекомендуется)
```json
"url": "https://mcp.tavily.com/mcp/?tavilyApiKey=tvly-ваш-ключ-здесь"
```
- **Где получить:** https://tavily.com
- **Бесплатно:** 1000 запросов/месяц
- **Качество:** Отличное (AI-powered search)

#### 📚 Context7
```json
"CONTEXT7_API_KEY": "ваш-ключ-здесь"
```
- **Где получить:** https://context7.com
- **Для чего:** Документация библиотек

#### 🚀 Render (опционально)
```json
"Authorization": "Bearer rnd_ваш-токен-здесь"
```
- **Где получить:** https://dashboard.render.com/account/settings

### Шаг 3: Перезапусти VS Code

Полный рестарт (не reload).

---

## 🎯 Архитектура поиска

### Два уровня:

1. **Tavily MCP** (основной)
   - Нативный SSE сервер
   - Лучшее качество результатов
   - 1000 запросов/месяц бесплатно
   - Инструменты: `tavily_search`, `tavily_extract`, `tavily_qna_search`

2. **unified-search** (fallback)
   - DuckDuckGo (бесплатно, без лимитов)
   - Автоматический fallback если Tavily недоступен
   - Инструменты: `unified_search`, `unified_research`

---

## 📊 Сравнение инструментов

| Инструмент | Провайдер | Лимиты | Качество | Когда использовать |
|------------|-----------|--------|----------|-------------------|
| `tavily_search` | Tavily | 1000/мес | ⭐⭐⭐⭐⭐ | Основной поиск |
| `tavily_extract` | Tavily | 1000/мес | ⭐⭐⭐⭐⭐ | Извлечение данных |
| `tavily_qna_search` | Tavily | 1000/мес | ⭐⭐⭐⭐⭐ | Q&A формат |
| `unified_search` | DuckDuckGo | Без лимитов | ⭐⭐⭐ | Fallback/массовый поиск |
| `unified_research` | DuckDuckGo | Без лимитов | ⭐⭐⭐ | Агрегация результатов |

---

## 🔧 Что работает без ключей

Эти серверы работают сразу:
- ✅ **unified-search** (DuckDuckGo режим)
- ✅ **git**
- ✅ **memory**
- ✅ **sequentialthinking**
- ✅ **filesystem**
- ✅ **fetch**
- ✅ **puppeteer**

---

## 📝 Где взять ключи

| Сервис | URL | Бесплатно | Приоритет |
|--------|-----|-----------|-----------|
| **Tavily** | https://tavily.com | 1000 req/month | 🔥 Высокий |
| Context7 | https://context7.com | Да | Средний |
| Render | https://dashboard.render.com | Да | Низкий |

---

## 🎯 Рекомендуемая настройка

### Минимум (работает сразу):
```json
{
  "tavily": { "url": "..." },  // Оставь плейсхолдер
  "unified-search": { ... }     // DuckDuckGo работает
}
```

### Оптимально:
```json
{
  "tavily": { "url": "...tvly-real-key" },  // Tavily ключ
  "unified-search": { ... },                 // DuckDuckGo fallback
  "context7": { "CONTEXT7_API_KEY": "..." } // Документация
}
```

---

## 🐛 Troubleshooting

### Tavily не работает
```bash
# Проверь URL в браузере (должен вернуть JSON)
https://mcp.tavily.com/mcp/?tavilyApiKey=tvly-your-key

# Проверь формат ключа
# Правильно: tvly-...
# Неправильно: pplx-... (это Perplexity)
```

### unified-search не работает
```bash
# Проверь Python зависимости
pip install duckduckgo-search

# Тест вручную
python scripts/unified_search.py search "test"
```

### Ошибка "Invalid JSON"
```bash
# Валидация JSON
python -m json.tool "C:\Users\admin\AppData\Roaming\GitLab\duo\mcp.json"
```

---

## 🎉 Готово!

После настройки:
1. ✅ Tavily для качественного поиска (1000 req/month)
2. ✅ DuckDuckGo для неограниченного fallback
3. ✅ Автоматическое переключение между провайдерами

**Скопируй `mcp.json.template` и заполни Tavily ключ!**
