# 👀 Как Посмотреть Landing Page Локально

## 🚀 Быстрый Старт

### 1. Запусти сервер:
```bash
cd C:\Workstation\frontend\landing
python -m http.server 8000
```

### 2. Открой в браузере:
```
http://localhost:8000
```

**Готово!** Landing page должна загрузиться.

---

## 📱 Посмотреть на Телефоне

### 1. Узнай IP адрес компьютера:
```bash
ipconfig
# Найди "IPv4 Address", например: 192.168.1.100
```

### 2. На телефоне открой:
```
http://192.168.1.100:8000
```

**Важно:** Телефон и компьютер должны быть в одной WiFi сети.

---

## ✅ Что Проверить

### Desktop:
- [ ] Страница загружается быстро (<2 сек)
- [ ] Анимации плавные (сетка, орбы, терминал)
- [ ] Hover эффекты работают (карточки, кнопки)
- [ ] Форма работает
- [ ] Скролл плавный

### Mobile:
- [ ] Адаптивный дизайн (одна колонка)
- [ ] Текст читаемый
- [ ] Кнопки удобные для тапа
- [ ] Нет горизонтального скролла

### Дизайн:
- [ ] Цвета не слишком яркие?
- [ ] Шрифты читаемые?
- [ ] Тёмная тема норм?
- [ ] Терминал понятный?

---

## 🎨 Быстрые Правки (Если Нужно)

### Цвета слишком яркие?
Открой `index.html`, найди `:root`:
```css
--accent-cyan: #00f0ff;     /* Сделай темнее: #00c0cc */
--accent-magenta: #ff006e;  /* Сделай темнее: #cc0055 */
```

### Анимации слишком быстрые?
```css
animation: gridMove 20s linear infinite;
/* Измени на: 30s или 40s */
```

### Орбы слишком размытые?
```css
.orb {
    filter: blur(80px);  /* Измени на: blur(60px) */
}
```

---

## 🐛 Проблемы?

### Страница не загружается?
```bash
# Проверь, что сервер запущен
# Должно быть: "Serving HTTP on :: port 8000"

# Попробуй другой порт:
python -m http.server 8080
# Открой: http://localhost:8080
```

### Телефон не подключается?
- Проверь, что на одном WiFi
- Попробуй отключить firewall
- Сначала проверь на компьютере

---

## 🚀 Когда Готов Деплоить

### 1. Останови сервер:
```bash
Ctrl+C
```

### 2. Если делал правки, закоммить:
```bash
git add .
git commit -m "Финальные правки"
git push github main
```

### 3. Деплой на Vercel:
```bash
# Вариант 1: CLI
cd frontend/landing
vercel --prod

# Вариант 2: Dashboard
# https://vercel.com/new
# Import: AMEOBIUS/Workstation
# Root: frontend/landing
```

---

## 📊 Тест Производительности

### Chrome DevTools:
1. F12 → Lighthouse
2. "Analyze page load"
3. Цель: 90+ по всем метрикам

---

**Сервер:** http://localhost:8000  
**Вопросы?** Спрашивай! 🤖
